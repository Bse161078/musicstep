import styled from "styled-components";

export const OrganizationDetailsFormStyle = styled.div`
    padding: 30px 0;
    display: grid;
    grid-template-columns: auto minmax(10px, 600px);
    justify-content: flex-start;
    grid-column-gap: 60px;
    border-bottom: 1px solid #c0c0c0;
    margin-bottom: 30px;

    .organization-details-form {
        display: grid;
        grid-row-gap: 30px;
    }
`