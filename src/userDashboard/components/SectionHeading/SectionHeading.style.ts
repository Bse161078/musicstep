import styled from "styled-components"

export const SectionHeadingStyle = styled.section`
    .section-heading {
        font-size: 24px;
        margin: 20px 0;
        font-weight: bold;
        color: #0C0C0C;
        
    }
`